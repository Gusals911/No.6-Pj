# Java/Maven
target/
*.class
*.jar
*.war
*.ear
*.log
.mvn/
mvnw
mvnw.cmd

# IDE
.idea/
*.iml
*.iws
*.ipr
.vscode/
.settings/
.classpath
.project
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
*.tmp

# Logs
*.log
logs/
app.log
app_startup.log

# Database
*.db
*.sqlite
*.sqlite3

# Application specific
TSEI_modeling/app.log
TSEI_modeling/app_startup.log
TSEI_modeling/target/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
ENV/
.venv
*.egg-info/
dist/
build/

# Modeling output data (large files - team should add these separately)
modeling/out/*
!modeling/out/.gitkeep
modeling/run/*.DAT
modeling/run/*.LST
modeling/run/*.CON
modeling/inp/restart/*.DAT
modeling/inp/surf/pr/*
modeling/inp/up/*.DAT

# Large binary files (exceed GitHub 100MB limit)
modeling/bin/*.exe
modeling/bin/wgrib2
modeling/util/skor.dem
modeling.zip
modeling_DB.zip

# Configuration files with sensitive data (keep examples, ignore actual configs)
modeling/util/*/config.json
!modeling/util/*/config*.example.json
!modeling/util/*/config.local.json.example

# Node modules (if any)
node_modules/
npm-debug.log
yarn-error.log

# Temporary files
*.tmp
*.temp
*.bak
*.backup

# Compressed files (usually too large)
*.zip
*.tar
*.tar.gz
*.rar
!modeling_DB.zip
!modeling.zip

# Environment variables
.env
.env.local
.env.*.local


# 추가해야 할 DB 설정파일
# .gitignore

### 1. Java / Maven 빌드 결과물 (자동 생성 파일) ###
# (용량이 크고, 개인 PC마다 달라 충돌의 원인이 됨)
/TSEI_modeling/target/
*.log

### 2. IDE 및 OS 설정 파일 ###
# (개인 개발 환경 설정이므로 제외)
.idea/
.vscode/
*.iml
.DS_Store

# ----------------------------------------------------
# [핵심] 모든 민감한 설정 "원본" 파일 제외
# (이 파일들은 GitHub에 올리지 않고, *.example 템플릿만 올립니다)
# ----------------------------------------------------

# Spring 설정 원본 파일 (템플릿: application.properties.example)
TSEI_modeling/src/main/resources/application.properties

# mdl(모델) 설정 원본 파일 1 (템플릿: config.json.example)
mdl/util/real-time_weather_emissions_transfer_3D/config.json

# mdl(모델) 설정 원본 파일 2 (템플릿: config.json.example)
mdl/util/insert_weather_modeling_to_DB_3D/config.json
